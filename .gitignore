# ─── config.php is committed intentionally (PRIVATE repo) so it
#     deploys with every push. ⚠ Rotate DB/SMTP passwords if this
#     repo is ever made public.

# ─── Local tooling / editor ───────────────────────────────
.claude/
.idea/
.vscode/

# ─── OS junk ──────────────────────────────────────────────
.DS_Store
**/.DS_Store
Thumbs.db
*.swp

# ─── Runtime / generated ──────────────────────────────────
logs/
logs/*
uploads/temp/

# ─── Large backup archive (not served by the site) ───────
assets/assets.zip
*.zip

# Local image backups (not deployed)
_originals/
